# OpenAI App Builder Checklist

- Endpoint `/mcp` is public and returns deterministic JSON.
- Response includes top-level `tools` and `endpoints` arrays.
- Auth endpoints remain available: `/api/login`, `/api/callback`, `/api/logout`.
